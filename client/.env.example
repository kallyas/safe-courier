# Base URL of the Safe Courier API.
# Leave unset in development to use the Vite dev proxy (/api -> backend).
VITE_API_URL=/api/v1

# Backend origin the dev proxy forwards /api requests to.
VITE_PROXY_TARGET=http://localhost:4500
